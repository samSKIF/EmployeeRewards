/**
 * TEMPORARY: Employee Core domain shim.
 * This re-exports from legacy @shared/schema to unblock migration.
 * Replace these re-exports with service-local models incrementally.
 */
// eslint-disable-next-line import/no-restricted-imports
export * from '@shared/schema';